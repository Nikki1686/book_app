<!DOCTYPE html>
<html lang="en">
<%- include('../../layout/head') %>

<body>
  <div class="wrapper">
    <%- include('../../layout/header') %>
  
    <%- include('../../layout/subheader', {
      message: 'Search Results',
      index: false
    }) %>
  
    <main>
      <% books.forEach((book, index) => { %>
        <%- include('../books/detail', {
          book: book,
          index: index
        }) %>
        <%- include('../books/edit', {
          book: book,
          index: index
        }) %>
      <% }) %>
    </main>

    <!-- This empty div is a CSS trick to keep the footer sticky at the bottom -->
    <div class="push"></div>
  </div>

  <%- include('../../layout/footer') %>
</body>
</html>