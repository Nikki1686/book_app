<!DOCTYPE html>
<html lang="en">
<%- include('../layout/head') %>

<body>
  <div class="wrapper">
    <%- include('../layout/header') %>
  
    <%- include('../layout/subheader', {
      message: 'Saved Books',
      index: true
    }) %>
    
    <main>
      <% books.forEach(book => { %>
          <%- include('books/detail', {book: book}) %>
      <% }) %>
    </main>
    <div class="push"></div>
  </div>

  <%- include('../layout/footer') %>
</body>
</html>